# Ảnh cũ không còn bắt buộc

Lab04 trước đây không có manifest ảnh thủ công và báo cáo tuyên bố không dùng placeholder. Từ phiên bản này, bảy ảnh trong `screenshot_manifest.py` là danh sách bắt buộc duy nhất. Không có PNG cũ nào bị xóa.
